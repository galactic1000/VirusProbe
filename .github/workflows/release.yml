name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        include:
          - os: windows-latest
            artifact_suffix: windows
            cli_name: VirusProbe-CLI.exe
            gui_name: VirusProbe-GUI.exe
            gui_mode: --onefile
            cli_icon: --icon gui/assets/icon.png
            gui_icon: --icon gui/assets/icon.png
          - os: ubuntu-latest
            artifact_suffix: linux
            cli_name: VirusProbe-CLI
            gui_name: VirusProbe-GUI
            gui_mode: --onefile
            strip: --strip
          - os: macos-latest
            artifact_suffix: macos
            cli_name: VirusProbe-CLI
            gui_name: VirusProbe-GUI.zip
            gui_mode: --onedir
            strip: --strip
            gui_icon: --icon gui/assets/icon.png

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-cli.txt
          pip install -r requirements-gui.txt
          pip install pyinstaller pillow

      - name: Build CLI executable
        run: >-
          pyinstaller --onefile --name ${{ matrix.cli_name }}
          ${{ matrix.strip }} ${{ matrix.cli_icon }} cli.py

      - name: Build GUI executable
        run: >-
          pyinstaller ${{ matrix.gui_mode }} --name VirusProbe-GUI --windowed
          ${{ matrix.strip }} --additional-hooks-dir=.github/pyinstaller_hooks
          --add-data "gui/assets/icon.png:gui/assets"
          ${{ matrix.gui_icon }}
          --exclude-module PIL._avif --exclude-module PIL._webp
          gui.py

      - name: Zip macOS GUI app bundle
        if: matrix.os == 'macos-latest'
        run: cd dist && zip -r ${{ matrix.gui_name }} VirusProbe-GUI.app

      - name: Upload CLI artifact
        uses: actions/upload-artifact@v4
        with:
          name: cli-${{ matrix.artifact_suffix }}
          path: dist/${{ matrix.cli_name }}

      - name: Upload GUI artifact
        uses: actions/upload-artifact@v4
        with:
          name: gui-${{ matrix.artifact_suffix }}
          path: dist/${{ matrix.gui_name }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure
        run: ls -R artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          cp artifacts/cli-windows/VirusProbe-CLI.exe release/
          cp artifacts/gui-windows/VirusProbe-GUI.exe release/
          cp artifacts/cli-linux/VirusProbe-CLI release/VirusProbe-CLI-linux
          cp artifacts/gui-linux/VirusProbe-GUI release/VirusProbe-GUI-linux
          cp artifacts/cli-macos/VirusProbe-CLI release/VirusProbe-CLI-macos
          cp artifacts/gui-macos/VirusProbe-GUI.zip release/VirusProbe-GUI-macos.zip

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
